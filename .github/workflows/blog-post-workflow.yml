name: Latest blog posts

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    runs-on: ubuntu-latest

    steps:
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "https://substack.com/feed/@knandwani07"
